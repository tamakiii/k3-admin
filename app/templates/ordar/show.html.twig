{% extends 'base.html.twig' %}

{% block title %}Ordar{% endblock %}

{% block body %}
    <h1>Ordar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordar.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ordar.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ordar_index') }}">back to list</a>

    <a href="{{ path('ordar_edit', {'id': ordar.id}) }}">edit</a>

    {{ include('ordar/_delete_form.html.twig') }}
{% endblock %}
